"use strict";
// Create a type alias called Contact that represents a contact card. It should include firstName and lastName as strings, 
// age as a number, and isOnline as a boolean.
const contact = {
    firstName: "Jane",
    lastName: "Doe",
    age: 30,
    isOnline: true,
};
