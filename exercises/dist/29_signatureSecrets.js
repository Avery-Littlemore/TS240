"use strict";
// Create an interface StringMap with an index signature that allows any number of string keys to be mapped to string values. 
// Then, create an object dictionary that adheres to StringMap and stores a couple of key-value pairs.
let dictionary = {
    book1: 'Book1',
    book2: 'Book2',
};
